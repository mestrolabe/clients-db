import { AddEditResolverDirective } from './add-edit-resolver.directive';

describe('AddEditResolverDirective', () => {
  it('should create an instance', () => {
    const directive = new AddEditResolverDirective();
    expect(directive).toBeTruthy();
  });
});
